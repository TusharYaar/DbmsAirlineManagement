<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dbms Flight Booking</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link    rel="stylesheet"    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"  />
    <link rel="stylesheet" href="./styles/preloader.css">
</head>
<body style="padding-top: 90px">
    <nav class="navbar navbar-dark bg-dark fixed-top">
        <a class="navbar-brand" href="/searchflight">
            <img src="./images/FullLogo.png" width="200" height="auto" alt="Airline Logo">
        </a>
        <% if (currentUser===null) {%> 
        <span class="navbar-text"><a href="login">Login</a> <a href="/register" class="mx-4"> Register </a> </span>
        <%} else {%>
            <span class="navbar-text"> logged in as: <%=currentUser %> <a href="/dashboard" class="mx-4">Dashboard</a> <a href="/logout" class ="mx-4">Logout</a>
            <% }%>
    
    </nav>
    <div class="container">
        <% if(error && error.length > 0) {%>
        <div class="alert alert-danger alert-dismissible fade show alert1" role="alert"> <%= error %> </div>    
            <%}%>
        <% if(success && success.length > 0) {%>
            <div class="alert alert-success alert-dismissible fade show alert2" role="alert"> <%= success %> </div>    
            <%}%>
        <% if(warning && warning.length > 0) {%>
            <div class="alert alert-warning alert-dismissible fade show alert3" role="alert"> <%= warning %> </div>    
            <%}%>            
    </div>
<div id="loader-wrapper">
        <div id="loader"></div>
        <div class="loader-section section-left"></div>
        <div class="loader-section section-right"></div>  
    </div>
<script> 
document.addEventListener("DOMContentLoaded",function(){
    document.getElementById("loader-wrapper").remove();
});</script>